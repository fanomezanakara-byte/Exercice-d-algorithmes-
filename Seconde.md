```text
Algorithme affichageDelheureetduminute


Variable
  var S : reel
  var M : reel
  var H : reel
Début
  Afficher("écrire le nombre de seconde" , S);
  lire (S);
  
  M<-S/60
  afficher (" la valeur du nombre en minute  est ",M);
  H<-S/3600
  afficher (" valeur du nombre en heure est", H);
  

Fin
